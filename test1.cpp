#include <iostream>
#include <cstdio>
#include <vector>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    long double res;
    vector<int> vt;
    for(int i=0;i<n;i++)
    {
        int temp;
        cin>>temp;
        vt.push_back(temp);
    }
    //for(int i=)
    return 0;
}